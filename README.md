Run the following command to obtain all the libraries:
sudo apt install libcurl4-gnutls-dev sqlite3 libsqlite3-dev

Run the build script to compile the project:
./build

Run the search executable with your search query to use the search engine:
./search [QUERY]
